# Lambda runtime already ships boto3 + botocore — these are listed here
# for local IDE resolution and CI linting only. Do NOT bundle into the
# deployment package; Lambda's managed runtime provides them.
boto3>=1.35
botocore>=1.35
